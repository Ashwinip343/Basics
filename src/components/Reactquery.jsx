import React from 'react'
//React Query - Fetching data in React
//fetch data from API , coz axios is not recommended and used coz it cannot work in strict mode(which is the good practice)

export const Reactquery = () => {
  return (
    <div>Reactquery</div>
  )
}
